dist:
  bionic

os:
- linux
- osx
osx_image: xcode11

addons:
  homebrew:
    packages:
    - bats-core
    - shellcheck
    - yq

language: minimal

before_install:
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo snap install yq; fi

script: true